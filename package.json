{
  "name": "date-fns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify main.js -s datefns -o date-fns.js && minify date-fns.js > min && cat modularize.js min > date-fns.min.js && rm min"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.28.0"
  }
}
